{
  "name": "job-hunt",
  "private": true,
  "workspaces": [
    "skill",
    "dashboard"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
